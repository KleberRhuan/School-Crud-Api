# Configurações do Ambiente
SPRING_PROFILES_ACTIVE=dev

# Configurações do Servidor
SERVER_PORT=8080

# Configurações do Banco de Dados
JDBC_DATABASE_URL=jdbc:postgresql://postgres:5432/esdras
JDBC_DATABASE_USERNAME=postgres
JDBC_DATABASE_PASSWORD=Teste123@
JDBC_DATABASE_HOST=postgres
JDBC_DATABASE_PORT=5432
JDBC_DATABASE_NAME=esdras
JDBC_DATABASE_SSLMODE=disable
JDBC_DATABASE_SSLROOTCERT=

# Pool de Conexões
DB_CONNECTION_TIMEOUT=30000
DB_IDLE_TIMEOUT=600000
DB_MAX_LIFETIME=1800000
DB_MAX_POOL_SIZE=10
DB_MIN_IDLE=2
DB_LEAK_DETECTION=60000

# Configurações do Tomcat
SERVER_TOMCAT_MAX_THREADS=200
SERVER_TOMCAT_MIN_SPARE_THREADS=10
SERVER_TOMCAT_CONNECTION_TIMEOUT=20000
SERVER_TOMCAT_KEEP_ALIVE_TIMEOUT=20000
SERVER_TOMCAT_MAX_CONNECTIONS=8192
SERVER_TOMCAT_ACCEPT_COUNT=100

# Configurações JWT
JWT_PUBLIC_KEY=classpath:certificates/public.pub
JWT_PRIVATE_KEY=classpath:certificates/private.pem
JWT_EXPIRATION=3600
JWT_ISSUER='http://localhost:8080'

# Configurações do Brevo (Email)
BREVO_API_KEY=sua_chave_api_brevo_aqui
BREVO_NAME='esdras research'
BREVO_EMAIL=kleberrhuan12@gmail.com
BREVO_URL=https://api.brevo.com/v3

# Configurações da Aplicação
ESDRAS_RESEARCH_URL=http://localhost:8080

# Rate Limiting
RATE_LIMIT_ENABLED=true
RATE_LIMIT_RPM=60
RATE_LIMIT_RPH=1000
AUTH_RATE_LIMIT_RPM=10

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080
CORS_ALLOWED_METHODS=GET,POST,PUT,DELETE,OPTIONS
CORS_ALLOWED_HEADERS=*
CORS_ALLOW_CREDENTIALS=true
CORS_MAX_AGE=3600

# Validação de Senha
PASSWORD_MIN_LENGTH=8
PASSWORD_REQUIRE_UPPERCASE=true
PASSWORD_REQUIRE_LOWERCASE=true
PASSWORD_REQUIRE_NUMBERS=true
PASSWORD_REQUIRE_SPECIAL=true

# Upload de Arquivos
FILE_MAX_SIZE=10MB
FILE_ALLOWED_TYPES=pdf,doc,docx,xls,xlsx,png,jpg,jpeg
FILE_TEMP_DIR=/tmp/esdras-uploads

# =============================================================================
# CONFIGURAÇÕES DE AUDITORIA POR AMBIENTE
# =============================================================================

# 🧑‍💻 DEVELOPMENT - Logs detalhados para debug
# AUDIT_ENABLED=true
# AUDIT_LOG_REQUESTS=true
# AUDIT_LOG_RESPONSES=true
# AUDIT_LOG_REQUEST_BODY=true
# AUDIT_LOG_RESPONSE_BODY=true
# AUDIT_LOG_ONLY_FAILURES=false
# AUDIT_MAX_BODY_SIZE=4096

# 🧪 STAGING - Balance entre info e performance
# AUDIT_ENABLED=true
# AUDIT_LOG_REQUESTS=true
# AUDIT_LOG_RESPONSES=false
# AUDIT_LOG_REQUEST_BODY=true
# AUDIT_LOG_RESPONSE_BODY=false
# AUDIT_LOG_ONLY_FAILURES=true
# AUDIT_MAX_BODY_SIZE=1024

# 🚀 PRODUCTION - Mínimo necessário, foco em falhas
# AUDIT_ENABLED=true
# AUDIT_LOG_REQUESTS=false
# AUDIT_LOG_RESPONSES=false
# AUDIT_LOG_REQUEST_BODY=false
# AUDIT_LOG_RESPONSE_BODY=false
# AUDIT_LOG_ONLY_FAILURES=true
# AUDIT_MAX_BODY_SIZE=512

# Padrão para desenvolvimento (descomente para usar)
AUDIT_ENABLED=true
AUDIT_LOG_REQUESTS=true
AUDIT_LOG_RESPONSES=false
AUDIT_LOG_REQUEST_BODY=true
AUDIT_LOG_RESPONSE_BODY=false
AUDIT_LOG_ONLY_FAILURES=false
AUDIT_MAX_BODY_SIZE=1024
AUDIT_SENSITIVE_HEADERS=Authorization,Cookie,X-API-Key,X-Auth-Token
AUDIT_IGNORED_PATHS=/actuator/**,/swagger-ui/**,/api-docs/**

# 🔥 TROUBLESHOOTING EMERGENCIAL - Ativar temporariamente em staging/prod
# export AUDIT_LOG_REQUESTS=true
# export AUDIT_LOG_REQUEST_BODY=true  
# export AUDIT_MAX_BODY_SIZE=2048

# Configurações de Validação (Adicionais)
PASSWORD_MAX_LENGTH=24
PASSWORD_UPPERCASE_COUNT=1
PASSWORD_LOWERCASE_COUNT=1
PASSWORD_NUMBERS_COUNT=1
PASSWORD_SPECIAL_COUNT=1

# Logs
LOG_FILE_PATH=/var/log/esdras-research/application.log